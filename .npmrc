# `apps/web` has a `prebuild` guard (scripts/check-icons.mjs) that must run for
# every `build`, including when the build is reached through a workspace filter
# from the root. pnpm only honours pre/post lifecycle scripts when this is on.
enable-pre-post-scripts=true
