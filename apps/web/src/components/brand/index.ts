/**
 * The brand surface (M18). One component, exported through a barrel so the
 * layouts that mount it match every other `@/components/*` import in the tree.
 */
export { BrandMark } from './brand-mark';
